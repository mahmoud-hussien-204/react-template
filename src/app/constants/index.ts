export const APP_CONSTANTS = {
  config: {
    defaultLanguage: "en",
    supportedLanguages: ["en", "ar"],
    defaultTheme: "light" as ITheme,
    supportedThemes: ["light", "dark", "system"] as ITheme[],
  },
}
